<h4>Add Comment</h4>
<%= form_for [@post, @post.comments.build], remote: true do |f| %>
  <%= f.text_area :body, rows: 4, :value => "", :placeholder => "Enter comment" %>
  <%= f.submit "ok" %>
<% end %>